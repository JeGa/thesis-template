\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{thesis-template}

\RequirePackage{graphicx}
\RequirePackage{kvoptions}
\RequirePackage{geometry}

\geometry{a4paper,top=2.5cm,left=2.6cm,right=2.6cm}

\SetupKeyvalOptions{
	family=tt,
	prefix=tt@
}

\DeclareStringOption[res]{logos}[res]
\DeclareStringOption[my-config]{config}[my-config]
\DeclareStringOption[my-abstract]{abstract}[my-abstract]
\DeclareStringOption[my-acknowledgment]{acknowledgment}[my-acknowledgment]

\ProcessLocalKeyvalOptions*

% All the template related configurations.
\frenchspacing

% User configuration.
\input{\tt@config}

% Provided commands.
\newcommand{\preface}{\input{preface}}
\newcommand{\toc}{\tableofcontents\pagenumbering{arabic}}

\endinput